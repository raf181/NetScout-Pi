# NetScout-Pi Requirements File
# Core Web Framework
flask==2.0.3
flask-socketio==5.1.1
python-dotenv==0.19.0
click==8.0.1
Werkzeug==2.0.3
Jinja2==3.0.3
MarkupSafe==2.0.1
itsdangerous==2.0.1

# WebSocket Support
eventlet==0.33.3
dnspython==2.3.0
bidict==0.22.1
six==1.16.0

# Network Tools
speedtest-cli==2.1.3
python-nmap==0.7.1
scapy==2.4.5
pyroute2==0.6.7
netifaces==0.11.0
psutil==5.8.0
ping3==4.0.4
netaddr==0.8.0

# Configuration and Data
# Note: For system-wide installation with sudo, install python3-yaml with apt:
#       sudo apt-get install python3-yaml
PyYAML==6.0
jsonschema==4.17.3
watchdog==2.1.5

# Security
bcrypt==3.2.2
PyJWT==2.4.0
cryptography==41.0.3

# HTTP and API
requests==2.28.2
urllib3==1.26.15
charset-normalizer==2.0.12

# Scheduling and Automation
python-crontab==2.5.1

# MQTT Support
paho-mqtt==1.5.1

# Data Analysis and Visualization
sqlalchemy==1.4.46
matplotlib==3.5.3
numpy==1.24.3  # Use numpy<2.0 to avoid compatibility issues
pandas==1.5.3  # Use pandas compatible with numpy<2.0
numpy<2.0.0  # Must be <2.0 for compatibility with matplotlib

# Utilities
setuptools==65.5.1
wheel==0.38.1
packaging==23.1
python-dateutil==2.8.2
typing-extensions==4.4.0
tzdata==2023.3
websocket-client==1.5.1
aiohttp==3.8.5

# System dependencies
# Make sure to have these installed via apt/system package manager:
# sudo apt-get install python3-dev python3-pip libffi-dev libssl-dev
# For specific plugins functionality:
# sudo apt-get install nmap arp-scan tcpdump iproute2 net-tools

# System setup (run these commands with sudo):
# sudo mkdir -p /var/log/netprobe /var/lib/netprobe /etc/netprobe/plugins
# sudo chown -R $USER:$USER /var/log/netprobe /var/lib/netprobe /etc/netprobe
# 
# Alternative method if using sudo to run the application:
# sudo mkdir -p /var/log/netprobe /var/lib/netprobe /etc/netprobe/plugins
# sudo chmod 777 /var/log/netprobe /var/lib/netprobe /etc/netprobe
