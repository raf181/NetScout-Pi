{
  "id": "wifi_scanner",
  "name": "Wi-Fi Scanner",
  "description": "Scans for nearby Wi-Fi networks and displays signal strength, channel information, and encryption types",
  "icon": "wifi",
  "parameters": [
    {
      "id": "interface",
      "name": "Wi-Fi Interface",
      "description": "The wireless interface to use for scanning",
      "type": "string",
      "required": true,
      "default": "wlan0"
    },
    {
      "id": "scan_time",
      "name": "Scan Time",
      "description": "Time in seconds to scan for networks",
      "type": "number",
      "required": false,
      "default": 5,
      "min": 1,
      "max": 30,
      "step": 1
    },
    {
      "id": "show_hidden",
      "name": "Show Hidden Networks",
      "description": "Include networks that don't broadcast their SSID",
      "type": "boolean",
      "required": false,
      "default": false
    }
  ]
}
